import Markup from "../Markup";

export default function Main() {
    return (
        <main>
            <section className="weather">
                <Markup />
            </section>
        </main>
    );
}
